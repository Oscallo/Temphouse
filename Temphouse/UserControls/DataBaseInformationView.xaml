<UserControl x:Class="Temphouse.UserControls.DataBaseInformationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Temphouse.UserControls"
             xmlns:vm="clr-namespace:Temphouse.ViewModels"
             xmlns:System="clr-namespace:System;assembly=mscorlib"
             xmlns:enums="clr-namespace:Temphouse.Enums"
             xmlns:ui="clr-namespace:CoreLand.UI.CustomControls;assembly=CoreLand.UI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="400">
    <UserControl.DataContext>
        <vm:DataBaseInformationViewModel />
    </UserControl.DataContext>
    <Grid Background="Transparent">
        <StackPanel Orientation="Vertical" HorizontalAlignment="Left">
            <StackPanel Orientation="Horizontal">
                <Label Content="Выберите тип базы данных:" FontWeight="Bold" Foreground="White"/>
                <ComboBox Height="20" Width="100" ItemsSource="{Binding DatabaseTypes}" SelectedItem="{Binding DatabaseType}"/>
            </StackPanel>
            <ui:EnumSelector ItemsSource="{Binding DatabaseTypes}" SelectedItem="{Binding DatabaseType}"/>
        </StackPanel>
    </Grid>
</UserControl>
