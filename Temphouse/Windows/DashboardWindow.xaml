<uiWindows:ExtendedWindow x:Class="Temphouse.Windows.DashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:ui="clr-namespace:CoreLand.UI.CustomControls;assembly=CoreLand.UI"
        xmlns:uiWindows="clr-namespace:CoreLand.UI.CustomControls.Windows;assembly=CoreLand.UI"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Temphouse.Windows"
        d:Template="{DynamicResource DefaultExtendedWindowTemplate}" 
        mc:Ignorable="d" IsHideable="True"
        Title="DashboardWindow" Height="450" Width="800" Icon="/Icon.ico">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="179*"/>
        </Grid.ColumnDefinitions>

        <Separator Grid.Column="1" Style="{DynamicResource VerticalSeparatorStyle}"/>
        <Grid Name="PART_MenuGrid" Style="{DynamicResource MenuGridStyle}">
            
        </Grid>
        <Border Name="PART_UserGrid" Grid.Column="2" Style="{DynamicResource UserBorderStyle}">
            
        </Border>
    </Grid>
</uiWindows:ExtendedWindow>
